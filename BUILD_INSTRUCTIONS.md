# تعليمات بناء APK المفصلة - Study Organizer

**الهدف**: بناء ملف APK قابل للتثبيت على أجهزة Android  
**الوقت المتوقع**: 10-15 دقيقة  
**المتطلبات**: Flutter SDK + Android SDK

---

## 📋 قائمة التحقق الأولية

قبل البدء، تأكد من:

- [ ] تثبيت Flutter SDK
- [ ] تثبيت Android SDK
- [ ] تثبيت Java Development Kit (JDK)
- [ ] توفر 5 GB من مساحة التخزين الحرة
- [ ] توصيل جهاز Android أو تشغيل محاكي

---

## 1️⃣ التحضير والإعداد

### الخطوة 1: التحقق من تثبيت Flutter

```bash
flutter --version
```

**النتيجة المتوقعة**:
```
Flutter 3.x.x • channel stable
Dart 3.x.x
```

### الخطوة 2: التحقق من متطلبات النظام

```bash
flutter doctor
```

**النتيجة المتوقعة**:
```
✓ Flutter (Channel stable, 3.x.x)
✓ Android toolchain - develop for Android devices
✓ Android Studio (version x.x.x)
✓ VS Code (version x.x.x)
✓ Connected device (1 available)
```

### الخطوة 3: الانتقال إلى مجلد المشروع

```bash
cd /path/to/flutter_study_organizer
```

---

## 2️⃣ تثبيت الاعتماديات

### الخطوة 1: الحصول على جميع الاعتماديات

```bash
flutter pub get
```

**ماذا يحدث**:
- تحميل جميع المكتبات المعرّفة في `pubspec.yaml`
- إنشاء ملف `pubspec.lock`
- قد يستغرق 2-5 دقائق

### الخطوة 2: التحقق من الاعتماديات

```bash
flutter pub outdated
```

**النتيجة المتوقعة**:
```
All dependencies are up to date!
```

---

## 3️⃣ تنظيف البناء السابق

### الخطوة 1: تنظيف المشروع

```bash
flutter clean
```

**ماذا يحدث**:
- حذف مجلد `build/`
- حذف الملفات المؤقتة
- تحرير مساحة التخزين

### الخطوة 2: الحصول على الاعتماديات مجددًا

```bash
flutter pub get
```

---

## 4️⃣ فحص الأخطاء

### الخطوة 1: تحليل الكود

```bash
flutter analyze
```

**النتيجة المتوقعة**:
```
No issues found!
```

### الخطوة 2: فحص الأخطاء والتحذيرات

```bash
flutter doctor -v
```

---

## 5️⃣ بناء APK

### الخيار 1: بناء APK في وضع Release (موصى به)

```bash
flutter build apk --release
```

**الميزات**:
- حجم أصغر (40-60 MB)
- أداء أفضل
- آمن للنشر

**الوقت المتوقع**: 5-10 دقائق

**النتيجة المتوقعة**:
```
✓ Built build/app/outputs/flutter-app.apk (XX MB)
```

### الخيار 2: بناء APK في وضع Debug

```bash
flutter build apk --debug
```

**الميزات**:
- أسرع في البناء
- يحتوي على معلومات التصحيح
- حجم أكبر (100-150 MB)

**الوقت المتوقع**: 2-5 دقائق

### الخيار 3: بناء APK منفصل لكل معمارية

```bash
flutter build apk --split-per-abi
```

**الميزات**:
- ملفات أصغر لكل معمارية
- تحميل أسرع على الأجهزة

**النتيجة**:
```
build/app/outputs/flutter-app-armeabi-v7a-release.apk
build/app/outputs/flutter-app-arm64-v8a-release.apk
build/app/outputs/flutter-app-x86-release.apk
build/app/outputs/flutter-app-x86_64-release.apk
```

---

## 6️⃣ موقع ملف APK

### بعد البناء بنجاح

```
build/app/outputs/flutter-app.apk
```

أو للبناء المنفصل:

```
build/app/outputs/flutter-app-armeabi-v7a-release.apk
build/app/outputs/flutter-app-arm64-v8a-release.apk
```

---

## 7️⃣ تثبيت APK على جهاز

### الخيار 1: التثبيت المباشر عبر Flutter

```bash
flutter install
```

**المتطلبات**:
- جهاز Android متصل أو محاكي مشغّل
- وضع المطور مفعّل على الجهاز

### الخيار 2: التثبيت عبر ADB

```bash
adb install build/app/outputs/flutter-app.apk
```

### الخيار 3: التثبيت اليدوي

1. نسخ ملف APK إلى جهاز Android
2. فتح مدير الملفات على الجهاز
3. النقر على ملف APK
4. تأكيد التثبيت

---

## 8️⃣ اختبار التطبيق

### بعد التثبيت

1. ابحث عن "Study Organizer" في قائمة التطبيقات
2. افتح التطبيق
3. اختبر الميزات الأساسية:
   - تسجيل الدخول
   - التنقل بين الشاشات
   - تحميل الصور والأيقونات
   - الإشعارات

---

## 9️⃣ بناء App Bundle (للنشر على Play Store)

### بناء App Bundle

```bash
flutter build appbundle --release
```

**الميزات**:
- تنسيق مقبول على Google Play Store
- حجم أصغر من APK
- يدعم التحسينات الديناميكية

**النتيجة**:
```
build/app/outputs/app-release.aab
```

---

## 🔟 معالجة الأخطاء الشائعة

### خطأ 1: "Flutter SDK not found"

```bash
# الحل:
flutter config --android-sdk /path/to/android/sdk
```

### خطأ 2: "Gradle build failed"

```bash
# الحل:
flutter clean
flutter pub get
flutter build apk --release
```

### خطأ 3: "Java not found"

```bash
# الحل:
# تأكد من تثبيت JDK 8 أو أحدث
java -version
```

### خطأ 4: "Android SDK not found"

```bash
# الحل:
# قم بتثبيت Android SDK من Android Studio
# أو حدّد مسار SDK في local.properties
```

### خطأ 5: "Compilation failed"

```bash
# الحل:
flutter clean
flutter pub get
flutter analyze
flutter build apk --release
```

---

## 1️⃣1️⃣ تحسينات الأداء

### تقليل حجم APK

```bash
# استخدم Release mode
flutter build apk --release

# استخدم Split APKs
flutter build apk --split-per-abi

# استخدم ProGuard
# (معرّف بالفعل في proguard-rules.pro)
```

### تسريع البناء

```bash
# استخدم Gradle daemon
# (معرّف بالفعل في gradle.properties)

# استخدم parallel builds
# (معرّف بالفعل في gradle.properties)
```

---

## 1️⃣2️⃣ الإعدادات المتقدمة

### تغيير رقم الإصدار

في `pubspec.yaml`:

```yaml
version: 1.0.0+1
         ↑     ↑
      الإصدار رقم البناء
```

### تغيير Application ID

في `android/app/build.gradle`:

```gradle
applicationId "com.study_organizer.study_organizer"
```

### تغيير اسم التطبيق

في `android/app/src/main/AndroidManifest.xml`:

```xml
android:label="Study Organizer"
```

---

## 1️⃣3️⃣ نصائح مهمة

### ✅ أفضل الممارسات

1. **استخدم Release mode دائماً** للإصدار النهائي
2. **اختبر على جهاز حقيقي** قبل النشر
3. **احتفظ بنسخة احتياطية** من ملف APK
4. **حدّث المكتبات بانتظام** للحصول على أحدث الميزات
5. **استخدم ProGuard** لتقليل حجم APK

### ⚠️ تجنب الأخطاء الشائعة

1. **لا تحذف مجلد `android/`** - مطلوب للبناء
2. **لا تغيّر `package name`** بعد النشر
3. **لا تستخدم Debug mode** للإصدار النهائي
4. **لا تنسَ تحديث الإصدار** قبل كل نشر
5. **لا تترك الملفات المؤقتة** - استخدم `flutter clean`

---

## 1️⃣4️⃣ الملفات الناتجة

### بعد البناء بنجاح

| الملف | الحجم | الاستخدام |
|------|-------|----------|
| `flutter-app.apk` | 40-60 MB | التثبيت المباشر |
| `app-release.aab` | 30-50 MB | نشر على Play Store |
| `flutter-app-arm64-v8a-release.apk` | 30-40 MB | أجهزة 64-bit |
| `flutter-app-armeabi-v7a-release.apk` | 25-35 MB | أجهزة 32-bit |

---

## 1️⃣5️⃣ الخطوات السريعة

### ملخص الأوامر

```bash
# 1. الانتقال إلى المشروع
cd /path/to/flutter_study_organizer

# 2. تنظيف البناء السابق
flutter clean

# 3. الحصول على الاعتماديات
flutter pub get

# 4. فحص الأخطاء
flutter analyze

# 5. بناء APK
flutter build apk --release

# 6. التثبيت على جهاز
flutter install
```

---

## 1️⃣6️⃣ الدعم والمساعدة

### موارد مفيدة

- [Flutter Documentation](https://flutter.dev/docs)
- [Android Developer Guide](https://developer.android.com/guide)
- [Gradle Documentation](https://gradle.org/guides)
- [Flutter Community](https://flutter.dev/community)

### الأوامر المفيدة

```bash
# عرض المساعدة
flutter build apk --help

# عرض معلومات النظام
flutter doctor -v

# عرض إصدار Flutter
flutter --version

# البحث عن الأجهزة المتصلة
adb devices
```

---

## 1️⃣7️⃣ الخلاصة

### ✅ تم إكمال البناء بنجاح!

بعد اتباع هذه الخطوات، سيكون لديك:
- ✅ ملف APK جاهز للتثبيت
- ✅ ملف App Bundle جاهز للنشر
- ✅ تطبيق مختبر وجاهز للاستخدام

### 🚀 الخطوة التالية

1. اختبر التطبيق على جهاز حقيقي
2. تأكد من جميع الميزات
3. أرسل التطبيق إلى المستخدمين
4. راقب الأداء والأخطاء

---

**آخر تحديث**: [التاريخ الحالي]  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للبناء
